<matches>
<% items.forEach(function (item) { %>
    <match>
        <<%= item.local %>><%= item.localgoals %></<%= item.local %>>
        <<%= item.away %>><%= item.awaygoals %></<%= item.away %>>
    </match>
<% }); %>
</matches>